import React from 'react';

const Rechart = ({ chartData }) => {
    console.log(chartData);
    const { name, totall } = chartData;
    return (
        <div>
            
        </div>
    );
};

export default Rechart;